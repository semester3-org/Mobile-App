<resources>

    <!-- style lain yg udah ada jangan dihapus, taruh di sini juga -->

    <style name="NeutralOutlineButton">
        <item name="android:background">@drawable/btn_outline_green</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:paddingLeft">20dp</item>
        <item name="android:paddingRight">20dp</item>
    </style>

    <!-- Base untuk chip filter -->
    <style name="FilterChipBase">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">36dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingLeft">18dp</item>
        <item name="android:paddingRight">18dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:background">@drawable/bg_btn_reset</item>
        <item name="android:textColor">@color/green_primary</item>
    </style>

    <!-- Chip aktif (hijau solid) -->
    <style name="FilterChipFilled" parent="FilterChipBase">
        <item name="android:background">@drawable/bg_btn_apply</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Chip non-aktif (outline hijau) -->
    <style name="FilterChipOutline" parent="FilterChipBase">
        <item name="android:background">@drawable/bg_btn_reset</item>
        <item name="android:textColor">@color/green_primary</item>
    </style>

    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowFullscreen">true</item>
    </style>


</resources>
